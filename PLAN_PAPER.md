# 论文写作计划

> 目标：完成一篇**应用/系统类**论文，核心贡献是「面向数控报警代码的多阶段混合检索故障诊断方法」。  
> 原则：先交专利、再投稿；论文只写能用实验证明的贡献，不把整仓工程功能当创新。  
> 配套文件：[`PLAN_PATENT.md`](PLAN_PATENT.md)（专利计划，须先完成申请日）。

---

## 0. 一句话定位

现有 hybrid RAG / GraphRAG 无法区分「报警代码 176」与「转速 176 rpm」。本文提出报警代码感知的多路检索与加权融合，辅以视觉结构化注入与图谱一跳扩展，并在真实 CNC 维修手册上用 Hit@K / MRR / 报警文档召回做对照与消融。

**不要写成：**「基于大模型的智能运维系统」。那是项目介绍，不是论文贡献。

---

## 1. 建议题目与投稿方向

### 1.1 题目（由窄到宽，选一）

1. **推荐：** 面向数控报警代码的多阶段混合检索故障诊断方法  
2. 视觉结构化增强与报警代码精确匹配的工业设备诊断检索方法  
3. A Multi-Stage Hybrid Retrieval Method for CNC Alarm-Code-Aware Fault Diagnosis  

英文题避免堆砌 LLM / ChatGPT / Agent。

### 1.2 投稿定位

| 档位 | 刊物类型 | 现实性 |
|---|---|---|
| 主投 | 计算机集成制造、机械工程学报、J. Intelligent Manufacturing、Computers in Industry | 高 |
| 备投 | IEEE Access、Applied Intelligence、CIMS 英文版 | 中高 |
| 不投 | ACL / NeurIPS / ICLR 方法轨 | 贡献粒度不够 |

篇幅按目标刊模板：中文约 8–12 页，英文约 10–14 页。

---

## 2. 只写三条贡献（全文贯穿）

1. **主贡献：** 报警代码独立成路（正则提取 + 报警上下文窗口过滤 + 报警代码表二次加权），再与向量、关键词按  
   \(S = 0.45\,K_w + 0.45\,V_v + 0.10\,G_b\) 融合。  
2. **次贡献：** 视觉模型输出保留「故障代码 / 描述 / 关键词」结构化字段，注入代码路与关键词路，而不是压成一段自由文本。  
3. **系统贡献：** 案例优先（置信度 ≥ 0.70 则跳过全库检索）+ SOP 首轮冻结的诊断闭环；用真实手册评测，不把管理端/JWT/主题当贡献。

明确**不写进贡献列表**的内容：Vue、FastAPI、RBAC、数字大屏、龙芯适配（可在 Discussion 一句带过）、LangGraph 框架本身、MySQL 图谱 8 条三元组（规模不够，见第 5 节）。

---

## 3. 现有材料怎么用

| 仓库文件 | 论文中的用途 | 用法 |
|---|---|---|
| `A1proj_real_api 2/DEFENSE_DRAFT.md` §1–3 | Introduction / 问题提出 | 改写成学术语气，删答辩套话 |
| `A1proj_real_api 2/PATENT_CORE_CONTENT.md` | Method 步骤与公式 | 改成「本文方法」，去掉专利口吻 |
| `A1proj_real_api 2/ARCHITECTURE.md` | 系统概览图（可选 1 张） | 只保留检索+生成，去掉管理端七面板 |
| `backend/app/langchain/rag_chain.py` | 实现对应、消融开关 | 实验必须对应该文件中的 stage |
| `backend/app/vision/vision_service.py` | 视觉 prompt 与字段 | Method 中引用输出格式 |
| `backend/app/knowledge/graph_builder.py` | JSON 文档图扩展 | 图谱贡献写「文档邻居扩展」，不写 MySQL 8 元组 |
| `软件功能需求分析文档.pdf` 等 | Related Work / 场景约束 | 不直接引用未公开 PDF，改写为领域描述 |

Method 必须以代码为准，不要只抄交底。当前 `retrieve()` 实际顺序：

```
case_search → vision_augment
  → 案例命中：跳过三路，手册仅作交叉验证
  → 案例未命中：parallel(vector | keyword | code_extract)
       → merge → alarm_code_boost → graph_expand → 第二轮 merge
       → 拼 context → LLM 四段式报告
```

论文图与实验开关都按此画，避免和专利附图编号混用（专利用附图1–3，论文用 Fig.1–N）。

---

## 4. 章节大纲与字数

| 节 | 内容 | 约字数（中文） | 完成标志 |
|---|---|---|---|
| 摘要 | 问题（报警码被向量淹没）+ 方法（六阶段）+ 1–2 个关键数字 | 300 | 不含「本系统实现了」 |
| 1 引言 | CNC 诊断痛点；三点缺陷；贡献 1/2/3；文章结构 | 1200 | 有对比文件式相关工作钩子 |
| 2 相关工作 | 工业诊断；hybrid RAG；GraphRAG；VLM+RAG；报警/日志检索 | 1500 | 每段末尾指出本文差异 |
| 3 方法 | 离线建库（短）+ 在线流水线（长）+ 融合公式 + 案例优先 | 3500 | 有算法伪代码 1 个 |
| 4 实验 | 数据、基线、指标、主表、消融、案例、误差分析 | 3000 | 至少 3 张表、2 张图 |
| 5 讨论 | 失败案例、阈值敏感性、与专利方法的工程取舍 | 800 | 承认图谱规模限制 |
| 6 结论 | 三条贡献复述 + 未来工作（预测性维护一句即可） | 400 | 不引入新实验 |

**算法伪代码建议只放一个：** Algorithm 1 `AlarmAwareHybridRetrieve(q, img, device)`，对应 `RAGChain.retrieve()`。

---

## 5. 实验计划（没有这节就不要投稿）

### 5.1 先建评测集（最高优先级）

从 `data/` 手册与报警代码表构造标注，**不要用对话演示代替**。

| 子集 | 规模 | 查询形态 | 标注 |
|---|---|---|---|
| A 报警代码查询 | ≥ 40 | 「176 什么问题」「主轴报警 250」 | 应对应报警代码表行 / 手册切片 ID |
| B 自然语言故障 | ≥ 40 | 「主轴发热停机」「伺服不使能」 | 1–3 条相关切片 |
| C 数值干扰 | ≥ 20 | 查询中同时含报警码与工艺数值 | 正确报警文档须排在工艺段落之前 |
| D 多模态（可选） | ≥ 15 | 文本 + 故障图（或截屏） | 视觉字段应抽出正确代码 |

每条样本字段：`query`, `device_model?`, `image?`, `gold_doc_ids[]`, `gold_alarm_code?`, `split=dev/test`。  
划分：dev 20% 调权重，test 80% 只报一次。

存放建议：`A1proj_real_api 2/eval/queries.jsonl`（新建，不进论文附录泄密手册全文）。

### 5.2 基线（必须跑）

| ID | 方法 | 对应实现 |
|---|---|---|
| B1 | 仅向量 | 只开 `_stage_vector` |
| B2 | 仅关键词 | 只开 `keyword_search` |
| B3 | 向量 + 关键词 | 两路 merge，无 code / graph |
| B4 | B3 + 代码提取（无 +15 增强） | `_stage_code_extract` |
| **Ours** | 全流水线 | 含 `alarm_code_boost` + `graph_expand` |

可选：BM25（可用现有 keyword 近似，文中声明差异）；若时间够再加「VLM 描述拼接后只做向量」。

### 5.3 消融（对应贡献）

| 去掉的模块 | 预期 |
|---|---|
| w/o code extract | 子集 A、C 的 Hit@1 明显下降 |
| w/o alarm_code_boost (+15) | 报警表文档进 Top-3 变差 |
| w/o graph expand | 子集 B 的 Hit@5 略降 |
| w/o vision structured fields | 子集 D 下降；自由文本拼接作对照 |
| w/o case-first | 时延上升；误复用用精确率/召回另表 |

### 5.4 指标

检索（主指标，审稿人最认）：

- Hit@1、Hit@5  
- MRR  
- **AlarmDoc@3**：gold 报警代码表文档是否进入 Top-3（子集 A/C 必报）

生成（辅指标，小样本人工即可）：

- 四段式完整率  
- 报警码是否写对  
- 步骤可执行性 Likert 1–5（2 名有维修背景的标注者，报一致率）

效率：检索时延、端到端时延（RAG vs Agent 可放附录，正文一句）。

### 5.5 代码改动（为实验，不是新功能）

在 `rag_chain.py` 为 `retrieve()` 增加开关，例如：

```text
use_vector, use_keyword, use_code, use_alarm_boost, use_graph, use_case
```

写 `scripts/eval_retrieval.py`：读 `eval/queries.jsonl`，打上述基线，输出 CSV。  
**不要**为发论文重写检索逻辑；权重 0.45/0.45/0.10 可在 dev 上敏感性分析（α∈{0.3,0.45,0.6}），test 只报默认值。

### 5.6 图谱怎么写才不会被打

当前 MySQL 因果三元组过少（答辩稿约 8 条），**禁止**作为核心实验证据。  
正文写：图谱扩展基于 JSON 文档图（`graph_builder` + `expand_doc_ids`）。MySQL 因果链放到 Limitations：「规模不足以支撑细粒度因果评测」。

---

## 6. 图表清单

| 编号 | 内容 | 来源 |
|---|---|---|
| Fig.1 | 在线检索流水线（与代码 stage 一致） | 重画，勿直接用专利附图 |
| Fig.2 | 加权融合与报警增强 | 公式树状图 |
| Fig.3 | 主结果柱状图（Hit@1/5，分子集 A/B/C） | 实验 CSV |
| Table 1 | 数据集统计（手册数、切片数、查询数） | sync 后 knowledge.json |
| Table 2 | 主结果 vs 基线 | eval 脚本 |
| Table 3 | 消融 | eval 脚本 |
| Table 4（可选） | 案例优先：准确率、误复用、时延 | case_service |

图中不要出现真实 RDS 地址、API Key、内部主机名。

---

## 7. 写作顺序（按周）

| 周 | 任务 | 产出 |
|---|---|---|
| 第 0 周 | **确认专利已递交或至少已确定申请日** | 与专利计划对齐 |
| 第 1 周 | 抽评测集 A/B/C；实现 eval 开关与脚本 | `queries.jsonl` + 可跑通 B1 |
| 第 2 周 | 跑完全部基线与消融；冻结 test 数字 | Table 2/3 初稿 |
| 第 3 周 | 写第 3 节方法 + Fig.1/2；Related Work 8–12 篇 | Method 定稿 |
| 第 4 周 | 写实验与误差分析；2 人小样本生成评估 | Results 定稿 |
| 第 5 周 | 引言、摘要、结论；按目标刊改格式 | 投稿稿 v1 |
| 第 6 周 | 同事互审：贡献是否被实验支撑 | 投稿稿 v2 |

第 0 周不可跳过：预印本、答辩公开、GitHub 公开均可能破坏专利新颖性。

---

## 8. Related Work 检索清单（写作前先读）

每类至少 2 篇，正文对比「本文差异」：

1. 工业/CNC 故障诊断与知识图谱  
2. Dense + sparse 混合检索、倒数融合 / 加权融合  
3. GraphRAG、文档图扩展  
4. 多模态 RAG / 工业视觉巡检  
5. 日志与告警代码检索（IT 运维可作类比，强调 CNC 数字码与工艺数值冲突）

不要大段引用 ChatGPT 白皮书。引用以可核验论文/标准为主。

---

## 9. 风险与否决条件

| 风险 | 处理 |
|---|---|
| 消融后代码路几乎无增益 | 先查评测集是否标错；再查报警表 `source` 是否含「报警代码及其解析」 |
| 向量一路已极强，融合无意义 | 强化子集 C（数值干扰）；这是本文存在理由 |
| 手册版权 | 论文只报统计与 ID，不粘贴手册原文 |
| 与专利表述冲突 | 方法步骤与专利一致，论文多写「为何有效」；阈值写成「本文取 α=0.45」而非唯一真理 |
| 审稿人认为工程堆砌 | 删掉管理端、权限、主题；Agent 最多半页 |

**否决投稿：** test 上 Ours 相对 B3 在子集 A 的 Hit@1 无提升，且子集 C 无提升。那时先改方法或改评测，不要硬投。

---

## 10. 完成检查表

- [ ] 专利申请日已确定（或代理书面确认可公开）  
- [ ] 题目不含「基于大模型的智能系统」空泛表述  
- [ ] 贡献恰好 3 条，且每条能在 Table 2/3 找到对应列  
- [ ] 评测集 ≥ 100 条查询，A/B/C 齐全  
- [ ] 基线 B1–B4 + Ours 数字来自同一脚本  
- [ ] MySQL 8 元组未当作图谱 SOTA  
- [ ] 无密钥、无内网地址、无未授权手册全文  
- [ ] 摘要中有至少一个检索指标数字  

写完初稿后，用一句话自检：**删掉系统实现，只留方法+表，还能否算一篇论文？** 若不能，继续砍工程、补实验。
