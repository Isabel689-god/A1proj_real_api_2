# 专利写作计划

> 目标：提交一件中国**发明专利**（方法 + 可选系统独权），保护报警代码感知的多模态多阶段检索增强生成方法。  
> 原则：**先申请、后论文/公开**；独权只保护步骤组合，不保护 Vue/登录/管理后台。  
> 已有底稿：`A1proj_real_api 2/PATENT_CORE_CONTENT.md`（约 70% 技术交底）。  
> 配套文件：[`PLAN_PAPER.md`](PLAN_PAPER.md)（论文须等本申请日之后）。

---

## 0. 一句话发明点

一种由计算机执行的故障诊断数据处理方法：将故障图像解析为**结构化字段**（代码、描述、关键词）而非自由文本，并行进行向量检索、关键词检索与**报警代码精确提取**，按加权公式融合，并对报警代码表文档二次加权，再沿知识图谱做有限邻居扩展，最后生成结构化诊断报告；向量嵌入与近邻检索经云端接口完成，从而不依赖本地 FAISS/GPU。

---

## 1. 申请策略

| 项 | 决定 |
|---|---|
| 类型 | 发明专利（软件方法不能走实用新型） |
| 件数 | **先做 1 件**；SOP 冻结若代理认为够创造性，再拆第 2 件 |
| 独权数量 | 方法 1 条 + 系统 1 条（系统引用方法步骤） |
| 不主张 | LLM 本身、LangChain/LangGraph 商标实现、前端框架、JWT、RBAC |
| 地域 | 先中国；PCT 等论文数字稳定、对比文件检索完成后再议 |
| 公开风险 | 仓库、答辩、预印本、演示视频均视为公开；申请日之前控制传播 |

与论文的分工：专利写「计算机如何做」；论文写「为什么更好」。步骤、公式符号与 `rag_chain.py` 保持一致，避免两套流水线。

---

## 2. 现有材料盘点

| 材料 | 状态 | 专利文件中的去向 |
|---|---|---|
| `PATENT_CORE_CONTENT.md` | 已有技术领域、附图说明、发明内容、效果 | 直接作为技术交底 v1，按第 4 节改产品名为上位概念 |
| `ARCHITECTURE.md` | 模块与部署 | 实施例 2（系统），删除具体 RDS 实例名 |
| `rag_chain.py` | 真实阶段与公式 `0.45/0.45/0.10`、`+15`、`Gb=0.6` | 实施例必须与代码可对应 |
| `vision_service.py` | 输出「故障代码/描述/关键词」 | 从权：结构化视觉注入 |
| `pipeline/` 抽取-校验-消重-入库 | 离线建库 | 独权前序步骤或从权 |
| `LOONGARCH_ADAPTATION.md` | 无本地 C++ 向量库 | 技术效果（4），不要写进独权限定死平台 |
| `patent_figures/` | **空目录** | 必须补黑白线条图 |
| 根目录五份 PDF | 需求/设计/测试 | 仅作内部对照，不作为对比文件 |

代理需要的一包文件：交底书、权利要求草案、附图、说明书摘要、发明人信息。本计划产出前三项的写作路径。

---

## 3. 权利要求骨架（先定权再写书）

### 3.1 独立权利要求 1（方法）——建议独权

一种面向工业设备故障诊断的多模态检索增强生成方法，由计算机设备执行，其特征在于包括：

**离线阶段（可压缩为 2～3 步写入独权，细节放从权）：**

1. 解析维修文档并分块，生成文档标识符；  
2. 构建知识图谱（文档节点与部件/故障/标签等邻居边）；  
3. 经网络接口将文本块向量化并写入云端向量数据库。

**在线阶段（独权核心，不可删）：**

4. 接收文本查询及可选故障图像；若存在图像，调用视觉模型得到**结构化字段**至少包括故障代码候选、故障描述与关键词列表，并据此生成增强查询字符串；  
5. 对增强查询并行或依次执行：云端向量近邻检索、关键词/二元组检索、**报警代码模式提取及报警代码表定位检索**；  
6. 按文档标识符合并多路结果，计算  
   \(S = \alpha K_w + \beta V_v + \gamma G_b\)（α、β、γ 为可配置正权重）；  
7. 若存在报警代码，对来源为报警代码表且内容命中该代码的文档给予额外加权并重排序；  
8. 以排序前列文档为种子，沿知识图谱扩展关联文档并再次按同一公式合并，输出前 K 条；  
9. 拼接上下文并调用大语言模型生成包含诊断、原因、方案的报告。

独权写法注意：

- 写「云端向量数据库 / 云端文本嵌入接口」，**不要**写 DashVector、DashScope、FAISS 产品名。  
- 写「工业设备 / 数控机床」，数控作为实施例。  
- α、β、γ、+15、窗口 ±80、K 写成**预设或可配置**，实施例再给 0.45/0.45/0.10。  
- 不要把「使用 LangGraph」写进独权。

### 3.2 从权清单（按创造性从高到低）

| 序号 | 限定 | 对应代码 |
|---|---|---|
| 2 | 视觉字段独立注入代码路与关键词路，而非仅拼接自由文本 | `_stage_vision_augment` + keyword 第二轮 |
| 3 | 数字报警码正则（孤立 2–4 位数字）与大写英文报警短语 | `_stage_code_extract` |
| 4 | 代码出现位置 ±N 字符窗口须含报警语境词，否则丢弃 | `alarm_ctx` 窗口 |
| 5 | 扫描报警代码表文档中「报警代码：{code}」格式并置顶/高分 | `keyword_score = 50` |
| 6 | 报警代码表二次加权 Ca（实施例 +15） | `_stage_alarm_code_boost` |
| 7 | 中文片段 + 二元组 + 精确短语加成 | `keyword_search` |
| 8 | 种子为合并结果前 M 篇，扩展上限 E，图谱增强 Gb 固定值 | `_stage_graph_expand` |
| 9 | 离线三元组抽取→校验→消重→关系数据库幂等写入 | `pipeline/` |
| 10 | 案例库相似度达阈值则跳过 5–8 步，手册仅交叉验证 | `_stage_case_search` |
| 11 | 整方法不依赖本地 GPU 与本地向量索引库 | 效果 + 实施例 |

SOP 首轮冻结、会话隔离：优先放**说明书实施例**，是否升从权等对比文件检索后再定。

### 3.3 独立权利要求 2（系统）

一种系统，包括：文档解析模块、图谱构建模块、向量索引模块、多阶段检索模块、报告生成模块，各模块执行权利要求 1 所述步骤。用于满足「方法 + 产品」布局，便于维权时主张系统。

不要再加「一种终端」「一种介质」除非代理坚持（中国发明可后补，首件保持简洁）。

---

## 4. 交底书修改清单（在 PATENT_CORE_CONTENT.md 上改）

按以下顺序出 **技术交底 v2**（给代理的版本）：

1. **去产品名：** DashVector → 云端向量数据库；DashScope / text-embedding-v3 → 云端文本嵌入服务；具体 LLM 型号改为「大语言模型接口」。实施例可保留「例如」一次。  
2. **去部署机密：** 删除任何 RDS 实例名、内网 IP、真实 Key、默认账号口令。  
3. **代码对齐：** 交底阶段顺序与 `retrieve()` 一致；写明案例优先为可选前置步骤（从权 10）。当前交底偏「六阶段」，代码还有 case_search——说明书用「优选实施例包含案例优先」。  
4. **魔法数上位化：** 独权用 αβγ、Ca、窗口 N、K；实施例给出当前实现值，并声明可依验证集调整。  
5. **对比文件段：** 补一节「现有技术」：纯向量 RAG；文本化 VLM+RAG；通用 GraphRAG。每条对应交底已写的四个技术问题。  
6. **技术问题保持四条：**（1）数字报警码与数值参数不可分；（2）多模态结构化信息丢失；（3）文档孤岛；（4）本地 FAISS/GPU 导致国产平台难部署。第 4 条服务创造性辅助，不要当成唯一发明点。  
7. **效果不要写死实验数字：** 专利阶段用「显著提升报警代码表文档排序优先级」即可；具体 Hit@K 留给论文，避免未做实验先写百分比。

交底结构维持：技术领域 → 背景问题 → 附图说明 → 发明内容（离线+在线）→ 具体实施方式 → 有益效果。

---

## 5. 附图计划（`patent_figures/` 现为空，必须补）

中国发明附图：**纯黑白线条、无照片、无灰度填充、无彩色**。与论文 Fig 分开绘制。

| 附图 | 内容 | 交底中的对应 |
|---|---|---|
| 附图 1 | 系统三层：数据层(100) / 检索层(200) / 生成层(300)，模块编号 101–108、201–207、301–302 | 已有文字描述，按此描图 |
| 附图 2 | 在线流程图：S201–S207、S301，含案例优先可选分支（虚线） | 与代码一致 |
| 附图 3 | 融合公式树：S、αKw、βVv、γGb | 已有 |
| 附图 4（建议增） | 报警代码提取与上下文窗口过滤（查询 → 正则 → 窗口判定 → 代码表命中） | 强化独权可辨识点 |
| 附图 5（可选） | 离线：解析→分块→抽三元组→校验消重→入库→图谱→云端索引 | 对应 A1–A7 |

文件命名建议：`patent_figures/fig1_architecture.eps`（或 SVG/PDF 线条稿）。先 SVG，代理再转官方格式。

检查：相同编号全文一致；不要把 Vue 组件名画进附图。

---

## 6. 说明书实施例怎么写

**实施例 1（方法，主实施例）：** 逐步对应 `RAGChain.retrieve` 与 `keyword_search` / `_merge_results`。给出一段假想查询「主轴报警 176」走过各阶段的中间结果形态（代码列表、是否命中报警表、融合后 Top-3 来源类型），**不要**粘贴真实手册原文。

**实施例 2（系统）：** 解析模块、检索模块、生成模块与数据流向，对应附图 1。硬件写「通用计算机 + 网络」，软件写「存储器中的指令」。

**实施例 3（国产化部署，可选）：** 无本地向量索引库、经 HTTP 调用云端嵌入与向量检索，适用于 LoongArch 等缺乏预编译向量库的环境。作为效果支撑，不作为独权必要特征（否则保护范围过窄）。

**反例 / 对比实施例（建议有）：** 仅向量检索时，查询「176」命中工艺参数段落；加入代码路与 +Ca 后命中报警代码表。这段对创造性和后续无效宣告都有用。

---

## 7. 对比文件检索（交代理前自己先做一轮）

检索式方向（CNKI / 专利检索系统）：

- 数控 AND 报警代码 AND 检索  
- RAG AND 故障诊断 AND 知识图谱  
- 多模态 AND 维修 AND 向量检索  
- hybrid retrieval alarm code / log retrieval  

记录 10 篇最接近的专利/论文：每篇「已公开什么 / 未公开什么（结构化视觉字段、报警窗口过滤、代码表二次加权）」。  
若某篇已覆盖「正则提报警码 + 向量 + 图谱」，则独权必须再叠「窗口过滤 + 代码表格式命中 + 二次加权」等区别特征。

---

## 8. 时间表（可与论文第 1 周并行准备，但公开必须在申请日之后）

| 阶段 | 工作 | 产出 | 建议周期 |
|---|---|---|---|
| P0 | 发明人、申请人、是否职务发明、保密 | 信息表 | 2 天 |
| P1 | 按第 4 节改交底；权利要求骨架定稿 | 交底 v2 + 权要草案 | 1 周 |
| P2 | 绘制附图 1–4 | `patent_figures/` | 与 P1 并行 |
| P3 | 对比文件 10 篇笔记；必要时收紧独权 | 检索备忘 | 3 天 |
| P4 | 代理正式申请文件；发明人核对步骤与代码 | 请求书+说明书+权要+附图 | 1–2 周（视代理） |
| P5 | **提交，记下申请号与申请日** | 电子回执 | — |
| P6 | 申请日之后：开放论文实验公开、预印本、答辩外传 | 与 `PLAN_PAPER.md` 第 0 周衔接 | — |

初审补正、实审意见（常见：技术方案属于智力活动规则、或与某 RAG 专利相同）：预先准备「报警代码与工艺数值在向量空间不可分」的技术问题陈述，用附图 4 + 反例实施例回答。

---

## 9. 与论文、开源的边界

| 行为 | 申请日前 | 申请日后 |
|---|---|---|
| 投论文 / arXiv | 否 | 可以 |
| GitHub 公开本仓库 | 否（至少不要公开 rag 核心与交底） | 可以，仍建议不提交 `.env` |
| 答辩、路演视频 `doc/` | 内部范围可控可；对外等于公开 | 可以 |
| 在论文中复述方法步骤 | 会破坏新颖性 | 可以，建议加「本申请已提交」脚注（按学校规定） |

论文实验数字**不要**写进专利申请文件（未完成易被认定为无法实现或事后补数据）。专利用定性效果即可。

---

## 10. 完成检查表

- [ ] 类型确认为发明专利，未误走实用新型  
- [ ] 独权能用「报警代码精确路径 + 结构化视觉 + 加权融合 + 图谱扩展」概括，且不含 UI 框架  
- [ ] 产品名已上位化；无密钥、无云实例 ID  
- [ ] 附图 ≥ 3 张黑白线条图，编号与说明书一致  
- [ ] 实施例步骤可在 `rag_chain.py` 找到对应函数  
- [ ] 魔法数在独权中为可配置，仅实施例给具体值  
- [ ] 对比文件笔记至少 10 篇，独权区别特征写明  
- [ ] SOP/Agent/管理端未进入独权  
- [ ] 申请回执已归档；论文计划第 0 周已勾选  

交给代理时附带一句话：**请将保护中心放在「数字报警代码与工艺数值的区分检索」，而不是「使用了大语言模型」。**
